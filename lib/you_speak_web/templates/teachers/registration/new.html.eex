<h1>Complete your registration</h1>

<%= form_for @changeset, Routes.registration_path(@conn, :create), fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= text_input f, :name, placeholder: "Name", class: "form-control" %>
  <%= error_tag f, :name %>

  <%= text_input f, :namespace, placeholder: "Namespace", class: "form-control" %>
  <%= error_tag f, :namespace %>

  <%= text_input f, :description, placeholder: "Description", class: "form-control" %>
  <%= error_tag f, :description %>

  <%= text_input f, :url, placeholder: "Url", class: "form-control" %>
  <%= error_tag f, :url %>

  <%= submit "Submit", class: "btn btn-primary" %>
<% end %>
